/* eslint-disable no-unused-vars */
import React from "react";

const FormToDoCartsElement = (arrayElement) => {
  
  return (
    <li style={{ color: "red" }} key={}>
      {title}
    </li>
  );
};

export default FormToDoCartsElement;
