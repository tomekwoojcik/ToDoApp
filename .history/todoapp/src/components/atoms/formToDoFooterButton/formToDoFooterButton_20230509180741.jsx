/* eslint-disable prettier/prettier */

const FormToDoFooterButton = el =>
    <Button className="formToDo__footerButton" variant="contained" key={1}>{el}</Button>;

export default FormToDoFooterButton;
