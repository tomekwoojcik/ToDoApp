import { Button } from "@mui/material";
import DeleteIcon from "@mui/icons-material/Delete";

const DeleteButton = (fun) => (
  <Button onClick={() => } endIcon={DeleteIcon}>
  </Button>
);

export default DeleteButton;
